---
version: alpha
name: Chronicle
description: Local-first versioned archive manager for arbitrary files and directories.
colors:
  primary: "#0f766e"
  primary-dark: "#115e59"
  primary-soft: "#e3f1ee"
  accent: "#c2410c"
  surface: "#ffffff"
  subtle: "#f6f8f7"
  hover: "#edf2f0"
  text-2: "#596762"
  text-3: "#7b8783"
  border: "#dde4e1"
  border-2: "#cbd5d1"
typography:
  sans:
    fontFamily: "Segoe UI Variable, Segoe UI, Noto Sans SC, sans-serif"
  mono:
    fontFamily: "Cascadia Code, Consolas, monospace"
---

## Overview

Chronicle 是面向任意文件和文件夹的本地优先存档管理器。它将用户选择的内容保存为可校验、可查询、可恢复的不可变时间节点，覆盖游戏存档、软件配置、创作工程和普通资料目录。界面采用紧凑、克制的桌面生产力工具风格，让条目、版本、同步状态和高风险操作始终清晰可辨。

## Product Contract

本地仓库是完整可用的数据源，断网或未配置远端时仍能创建、查询、校验和恢复快照。WebDAV 是可选副本和多设备交换层，不是本地操作的前置条件。

用户选择的单个文件或目录统一建模为 `Entry`。某一时刻的完整内容统一建模为 `Snapshot`。快照只追加，不原地修改；分类属于本地视图，不参与内容寻址。

恢复、删除、远程清理和冲突处理必须可确认、可追踪。恢复现有源内容前先创建安全快照。同步冲突形成可见分支，由用户选择结果，不静默覆盖任一设备版本。

首个稳定版本支持手动备份、计划备份、文件变化触发、退出时备份、分类与查询、本地恢复、WebDAV 手动和自动同步、保留策略、完整性检查及导入导出。分块去重、加密仓库、S3 和 SMB 在基础闭环稳定后评估。

## Colors

深青色承担品牌、导航选中态、恢复等可信操作；橙色只用于当前页面最重要的创建动作。表面与分隔色保持低对比层级，错误、警告和成功状态必须同时使用文字或图标表达，不能只依赖颜色。

## Typography

界面使用系统无衬线字体栈，数据哈希使用等宽字体。标题、正文、辅助信息和数据标签维持稳定层级；路径、名称和哈希过长时允许截断，并提供查看完整值的交互入口。

## Layout

桌面主界面保持三层信息结构：左侧为资料库与分类，中间为条目列表，右侧为条目详情。详情区继续分为时间线和已选版本检查器。窗口缩小时优先压缩次要操作与检查器宽度，不隐藏创建快照、查看历史和恢复入口。

左侧导航只承载范围切换，创建存档是独立主操作。中间列表支持分类筛选和关键词查询。右侧头部展示当前条目身份及动作，主体按时间倒序展示快照，检查器展示校验、容量和变更统计。

## Components

主要按钮每个视图只保留一个，次要动作使用描边或文本按钮。图标统一使用 Lucide 线性图标；仅含图标的按钮必须提供可访问名称，带可见文字的装饰图标不重复传达信息。

耗时操作必须禁用重复提交并显示阶段或进度。成功和普通提示自动消失，错误提示说明具体失败原因。空状态同时说明当前状态和可执行动作。恢复等覆盖性操作在执行前显示目标版本，并明确安全快照行为。

## Do's and Don'ts

保持本地优先、不可变快照和显式版本格式。新存储后端通过端口接入，不让远端协议进入领域模型。所有对象在发布清单前完成写入和哈希校验。所有异步任务持久化阶段、进度和错误，允许应用重启后继续。

不要让 Vue 界面直接操作 SQLite、归档文件或 WebDAV。不要用界面版本推断仓库格式。不要在文件监听回调中直接执行备份。不要静默覆盖远端分支，不要在未校验对象时发布快照清单，不要将分类写入内容地址。

## System Architecture

`crates/chronicle-core` 负责领域类型、用例边界和跨适配器错误语义，不依赖 Tauri、SQLite、压缩库或 WebDAV。`crates/chronicle-storage` 负责本地清单、对象归档、校验、恢复和 SQLite 索引适配。`crates/chronicle-sync` 负责差异计算、同步任务状态机、传输重试和远端适配。`apps/desktop` 负责 Tauri 2 外壳、Vue 3 界面及系统集成，只通过命令层调用应用用例。

界面包含一个基于 File System Access API 和 IndexedDB 的浏览器适配器，用于网页开发和交互测试。Tauri 环境通过独立适配器调用 Rust 命令，文件内容不经过前端 IPC，统一保存到系统应用数据目录中的本地仓库。Rust 文件系统仓库实现版本化 JSON 清单、7z 时间线、SHA-256、差异统计、完整性检查、恢复前安全快照和原子替换。

应用设置分为软件、存储与备份、热键和关于；WebDAV 配置由标题栏中的独立云端设置入口承载。界面偏好使用版本化本地配置保存，能够立即生效的默认分类、初始快照和热键由 Vue 应用层读取；系统启动、托盘、计划备份和云端连接在对应桌面服务接入后由命令层执行。

SQLite 只保存可重建的索引和运行状态。归档对象与版本化 JSON 清单独立存放；索引损坏时通过扫描清单重建。存储和同步面向接口编程，以支持内存测试适配器及后续远端扩展。

## Repository Format

本地应用数据目录和远端存储根目录统一使用 `Chronicle` 文件夹。当前仓库格式为 V2：

```text
Chronicle/
├─ config/
│  └─ settings.json
└─ data/
   ├─ catalog.json
   ├─ entries/
   │  └─ <可读名称>__<稳定存档ID>/
   │     ├─ entry.json
   │     ├─ timeline.json
   │     └─ 20260907T142530.123Z_<快照ID前缀>.7z
   └─ .tmp/
```

`config/settings.json` 是格式版本化的软件设置文档，固定包含 `formatVersion`、`app` 和 `cloud`。密码和访问令牌不写入该文件，凭据接入系统保险库后单独管理。`data/catalog.json` 保存仓库级入口、统计摘要和由稳定 ID、`parentId` 组成的嵌套分类树；`entry.json` 保存存档名称、分类 ID、保存策略及来源；`timeline.json` 保存按时间排列的快照元数据、SHA-256、文件清单和变更统计。

一个存档可以组合多个文件和文件夹。每个来源分配稳定 ID，7z 内部以该 ID 建立命名空间，解决来源同名和多设备位置不同的问题。快照文件名采用 UTC 时间前缀，按文件名即可得到时间顺序；快照先写入 `.tmp`，完成压缩与哈希后再发布元数据。恢复前创建安全快照，并在替换源内容前校验 7z 的 SHA-256。

WebDAV 和后续 GitHub 仓库方案实现同一个远端存储接口，只负责列举、读取、写入和原子发布仓库对象。同步先读取一次 `catalog.json`，只读取发生差异的 `timeline.json`，利用 ETag 或修改时间跳过未变化清单，避免扫描和逐文件探测整个远端目录。上传和下载共用一个队列，默认合计并发 2；元数据读取默认并发 2；请求启动间隔默认 150 毫秒。重复的自动同步触发合并为一次后续同步，不为每个文件变化单独启动远端扫描。

远端返回 408、425、429、500、502、503 或 504 时，传输层优先采用 `Retry-After`，否则使用带抖动的指数退避，默认最多重试 5 次。7z 对象完成传输和 SHA-256 校验后才更新远端清单，失败对象保留在任务队列中，不通过高频完整重扫恢复。同步采用后台任务、显式进度、压缩级别和完整性元数据；多设备路径映射归设备配置所有，不以某台机器的绝对路径覆盖其他设备。该设计吸收 RGSM Archive V2 的稳定存档单元标识、多位置映射、后台同步和完整性校验原则。

## Delivery Stages

1. 本地闭环：条目、分类、手动快照、查询、校验和恢复。Vue 双适配器、Rust 文件系统仓库和 Tauri 命令桥已完成；SQLite 索引待完成。
2. 自动化与维护：计划任务、退出时备份、保留策略、完整性扫描、索引重建及导入导出。
3. WebDAV：仓库配置、连接测试、手动同步、差异预览、断点重试和冲突展示。
4. 持续同步：文件变化防抖、自动快照、自动同步、多设备分支和远程清理。
5. 扩展评估：按真实仓库数据决定是否加入分块去重、端到端加密、S3、SMB 和其他后端。

## Definition of Done

每个里程碑同时通过领域单元测试、适配器集成测试和真实临时目录端到端测试。前端必须通过 TypeScript 检查、组件测试和生产构建；Rust 必须通过 rustfmt、Clippy 与工作区测试；`main` 分支的 GitHub Actions 必须全部成功。

涉及磁盘覆盖的测试必须验证原内容可由安全快照恢复。涉及远端提交的测试必须覆盖中断、重试、重复执行和对象损坏。格式变化必须更新显式版本及迁移测试。完成状态以可运行行为和自动测试为准，不以按钮或静态界面存在为准。
