# Progress — Chronicle 1.1.0 OpenDAL

Plan: 2026-09-09-opendal-v1.1.md
Branch: feature/opendal-v1.1.0
Base: 5d95ebe
Worktree: F:/project/Chronicle-opendal-v1.1

## Tasks

- Task 1: complete — OpenDAL source adapter, settings v3 migration, credential gating, UI templates and README implemented.
- Task 2: complete — independent review found and corrected GCS credential encoding plus local-credential alias protection.
- Task 3: complete — source-only verification passed; no installer or Release created.

## Verified baseline

- cargo test --workspace: 15 tests pass.
- npm test: 19 files / 65 tests pass.
- npm run typecheck: passes.
- cargo search/info confirms OpenDAL 0.59.1 released, minimum Rust 1.91; repository uses 1.97.

## Decisions

- Existing sources and credentials remain compatible, both legacy adapters retained.
- Object storage does not need atomic rename; metadata must be published after objects, with failure behavior verified.
- No real user cloud credentials or historical conversation tokens used for verification.
- Final verification: `cargo test --workspace` (19 tests), `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml` (7 tests), `npm test` (20 files / 78 tests), `npm run typecheck`, `npm run build`, and `git diff --check` all pass.
- Review correction: GCS service-account JSON is converted to the base64 value required by OpenDAL 0.59.1; ambient, VM and file credential loading are disabled, including the `google_application_credentials` alias.
